# 💰 Personal Finance AI  
Интеллектуальная система анализа личных финансов  
ML + FastAPI Backend + Insights Engine

---

## 🚀 О проекте

**Personal Finance AI** — это система умного анализа финансов, созданная для того, чтобы пользователи могли лучше понимать свои расходы, доходы и финансовое состояние.

Проект включает:

- автоматическую категориацию транзакций (rules + ML),
- умные рекомендации,
- финансовые инсайты,
- расчёт финансовой подушки,
- API для frontend-приложения.

---

## 🧠 ML Модели

Мы используем **гибридную модель**:

### 1. 🔹 Rule-based engine  
Определяет очевидные случаи:
- подписки  
- транспорт  
- еда  
- магазины  
- доходы  
- большие переводы  

### 2. 🔹 ML классификатор (Logistic Regression + TF-IDF + numeric features)

Модель обучена на:
- реальных данных `ci_data.csv`
- синтетическом датасете (балансировка)
- расширенных категориях  
- новых признаках (amount, abs_amount, is_income)

### Итоговые категории:
- `food` — Еда  
- `transport` — Транспорт  
- `stores` — Магазины  
- `monthly` — Ежемесячные платежи  
- `income` — Доход  
- `other` — Прочее  

---

## 📦 Структура проекта

- `main.py` — FastAPI-приложение и описание схем запросов/ответов.
- `ml/` — вспомогательные модули: правила классификации, ML-модель, генерация инсайтов и прогноза.
- `data/` — исходные данные для обучения/аналитики.
- `backend_models/` — сериализованные артефакты модели.
- `requirements.txt` — перечень зависимостей Python.

---

## 🔧 Как запустить и протестировать Backend

### Вариант 1. Локально (виртуальное окружение)

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

# Запуск dev-сервера
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Проверка здоровья:

```bash
curl http://localhost:8000/health
```


### Вариант 2. Docker/Docker Compose

```bash
docker compose up --build -d
# или вручную
docker build -t finance-ai .
docker run --rm -p 8000:8000 finance-ai
```

API поднимется на `http://localhost:8000`. В репозитории есть unit-файл `finance-ai.service` для systemd, если нужно
держать compose-сервис в проде.

### Вариант 3. Деплой через systemd + Docker Compose

Если репозиторий расположен по пути `/home/devops/finance-ai`, unit-файл уже содержит правильные пути для Docker Compose.

```bash
# из корня репозитория
sudo cp finance-ai.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now finance-ai.service

# проверка, что контейнер поднялся и отвечает
curl http://localhost:8000/health
```

Если проект находится в другом каталоге, обновите `WorkingDirectory` и пути в `ExecStart`/`ExecStop` в `finance-ai.service`
перед копированием.

### 3) Пример запросов к API

**Классификация транзакции**

```bash
curl -X POST http://localhost:8000/classify \
  -H "Content-Type: application/json" \
  -d '{
        "ref": "Supermarket purchase",
        "withdraw": 1200,
        "deposit": 0
      }'
```

**Инсайты по бюджету**

```bash
curl -X POST http://localhost:8000/insights \
  -H "Content-Type: application/json" \
  -d '{
        "transactions": [
          {"date": "2024-12-01", "ref": "Groceries", "withdraw": 3000, "deposit": 0},
          {"date": "2024-12-02", "ref": "Salary", "withdraw": 0, "deposit": 90000}
        ],
        "monthly_budget": 60000,
        "current_balance": 150000,
        "avg_daily_expense": 2500
      }'
```

**Прогноз баланса**

```bash
curl -X POST http://localhost:8000/forecast
```

### 4) Что вернёт Backend

- `/classify` — источник решения (rules/ML), внутренняя и человеческая категории, уверенность, флаг необходимости ручной проверки.
- `/insights` — сводка по категориям, статус бюджета, размер финансовой подушки и текстовые рекомендации.
- `/forecast` — список дат с прогнозом баланса от функции `generate_forecast`.

---

## 🔜 Что делать дальше

1. **Проверьте артефакты модели.** В каталоге `backend_models/` уже лежат сериализованные веса; убедитесь, что файлы доступны приложению при запуске (иначе переобучите модель из `ml/`).
2. **Подготовьте переменные окружения.** Если сервер запускается в Docker/на удалённом хосте, сразу задайте `PORT`, `HOST` и при необходимости путь к моделям (`MODEL_DIR`).
3. **Соберите автотесты.** Выполните быструю проверку синтаксиса `python -m compileall main.py ml` перед деплоем.
4. **Интегрируйте с фронтендом.** Используйте REST-эндпоинты из раздела выше; для визуализации бюджета и прогноза подключайте `/insights` и `/forecast` соответственно.
5. **Настройте мониторинг.** Добавьте пинг `/health` в систему мониторинга и журналы запросов, чтобы вовремя отслеживать ошибки классификатора или деградацию модели.

---

## 🐳 Запуск в Docker

1. Соберите образ (из корня репозитория):

```bash
docker build -t finance-ai .
```

2. Запустите контейнер, пробросив порт API наружу:

```bash
docker run --rm -p 8000:8000 finance-ai
```

API будет доступен на `http://localhost:8000` с эндпоинтом здоровья `/health`.